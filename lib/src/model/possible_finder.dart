part of model;

class PossibleFinder{
  CurrentState state;

  List<int> findPossiblesForBlackPiece(int position){
    Arrangement arrangement = state.arrangement;
//    if(state)
//    if(state.)
  }

}